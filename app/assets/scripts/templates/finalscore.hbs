<p class="questions__score__intro">Your score:
  {{numberCorrect}} / {{quizLength}}</p>

{{#each answers}}
<ul>
  <li class="questions__score">
    {{this.questionNumber}} - {{this.answerText}}
      {{#if this.isCorrect}}
        <span class="questions__score--correct">✔</span>
      {{else}}
        <span class="questions__score--incorrect">X</span>
      {{/if}}
  </li>
</ul>
{{/each}}
